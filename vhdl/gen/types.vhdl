-- generated from /workspaces/hpc/scripts/generate.js
LIBRARY ieee;
PACKAGE types IS
    -- The message should be a matrix of k*k, which is 16-bits, for ideal, the message should be serially input

    -- MSG_LENGTH(k) = 4
    CONSTANT MSG_LENGTH      : INTEGER := 3;

    -- CODEWORD_LENGTH(n) = 4
    CONSTANT CODEWORD_LENGTH : INTEGER := 6;

    -- CHECK_BITS = 3
    CONSTANT CHEKC_BITS      : INTEGER := 2;
    TYPE GEN_MAT IS ARRAY (0 TO MSG_LENGTH, 0 TO CODEWORD_LENGTH) OF BIT;
    TYPE CHK_MAT IS ARRAY (0 TO CHEKC_BITS, 0 TO CODEWORD_LENGTH) OF BIT;
    TYPE MSG_LINE IS ARRAY (0 TO MSG_LENGTH) OF BIT;
    TYPE MSG_MAT IS ARRAY (0 TO MSG_LENGTH) OF MSG_LINE;
    TYPE MSG_SERIAL IS ARRAY (0 TO 15) OF BIT;
    TYPE CODEWORD_LINE IS ARRAY (0 TO CODEWORD_LENGTH) OF BIT;
    TYPE CODEWORD_MAT IS ARRAY(0 TO CODEWORD_LENGTH) OF CODEWORD_LINE;
    TYPE CODEWORD_SERIAL IS ARRAY (0 TO 48) OF BIT;
END PACKAGE types;
